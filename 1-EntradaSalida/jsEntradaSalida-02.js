/*
Debemos lograr tomar un nombre con 'prompt' 
y luego mostrarlo por 'alert' al presionar el botón  'mostrar'*/
function mostrar()
{
	/*let nombre = "Samuel"; 
	tambien se puede usar let=var son los mismos
	declaré la variable e Inicie que su valor es Samuel
	let nombre;
	nombre = "Samuel";

	alert(nombre);
	pasa un alert con samuel y despues como volvi a aclarar
	la variable como raom tira despues ramon 
	nombre ="Ramon";

	alert(nombre);*/
	/*La variable va a ser un LET y declaro la variable con el pront del dato que el usuario ingrese*/ 
	let nombre;
	
	nombre = prompt("Ingrese nombre");

	alert(nombre);
}

